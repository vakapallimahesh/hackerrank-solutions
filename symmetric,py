from itertools import combinations

# Input
s, k = input().split()
k = int(k)

# Sort the string
s = sorted(s)

# Generate combinations from size 1 to k
for i in range(1, k + 1):
    for comb in combinations(s, i):
        print("".join(comb))
